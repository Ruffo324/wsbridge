# Builds the HA add-on image for amd64 / aarch64 / armv7 and pushes to GHCR.
#
# Repo: https://github.com/Ruffo324/wsbridge
#
# One-time setup after the first push:
#   1. Settings → Actions → General → enable "Read and write permissions".
#   2. After the first successful run, set the GHCR package visibility under
#      your GitHub profile → Packages.

name: Build add-on

on:
  push:
    branches: [master]
    paths:
      - "addons/https2wss/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "Dockerfile"
      - ".github/workflows/addon-build.yml"
  workflow_dispatch:
    inputs:
      version:
        description: "Override version tag (else uses addons/https2wss/config.yaml version)"
        required: false
        type: string

permissions:
  contents: read
  packages: write     # push images to GHCR

jobs:
  build:
    name: Build ${{ matrix.arch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, aarch64, armv7]
    steps:
      - uses: actions/checkout@v4

      - name: Read add-on version
        id: meta
        shell: bash
        run: |
          VERSION=$(grep '^version:' addons/https2wss/config.yaml | awk '{print $2}' | tr -d '"')
          EFFECTIVE="${{ inputs.version }}"
          if [ -z "$EFFECTIVE" ]; then
            EFFECTIVE="$VERSION"
          fi
          # Normalize repo owner to lowercase — GHCR requires lowercase namespaces.
          OWNER_LC=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "version=$EFFECTIVE" >> "$GITHUB_OUTPUT"
          echo "owner=$OWNER_LC" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build add-on image
        uses: home-assistant/builder@2026.06.0
        with:
          args: |
            --${{ matrix.arch }}
            --target addons/https2wss
            --image "ghcr.io/${{ steps.meta.outputs.owner }}/{arch}-https2wss"
            --version ${{ steps.meta.outputs.version }}
            --addon
